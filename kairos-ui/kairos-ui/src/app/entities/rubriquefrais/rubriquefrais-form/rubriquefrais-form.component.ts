// File generated by Telosys Tools Generator ( version 3.1.2 ) - Date 2020-04-27 ( Time 15:44:35 )

// Modules imports
import { NotificationService } from './../../../services/notification.service';
import { FormBuilder, FormGroup, Validators } from '@angular/forms';
import { MatDialog } from '@angular/material';
import { ActivatedRoute, Router } from '@angular/router';
import { Component, OnInit } from '@angular/core';

// Components imports
import { DeleteDialogComponent } from './../../../delete-dialog/delete-dialog.component';

import { DatePipe } from '@angular/common';

// Models and services imports
import { Rubriquefrais } from './../rubriquefrais.model';
import { RubriquefraisService } from './../services/rubriquefrais.service';
import * as _ from 'underscore';

// Pour gestion des listes
import { ListeService } from './../../../entities/liste/services/liste.service';
import { Liste } from './../../../entities/liste/liste.model';

import { Lov } from './../../../common/lov';
import {SelectItem} from 'primeng/api';

@Component({
  selector: 'app-rubriquefrais-form',
  templateUrl: './rubriquefrais-form.component.html',
  styleUrls: ['./rubriquefrais-form.component.css'],
  providers: [DatePipe]
})
export class RubriquefraisFormComponent extends Lov implements OnInit {

  // HTTP status code
  readonly NOT_FOUND_ERROR = 404;
  readonly CONFLICT_ERROR = 409;
  readonly INTERNAL_SERVER_ERROR = 500;

  public title = 'Formulaire Rubriquefrais';
  public form: FormGroup;

  // Définition des filtres

  public rubriquefrais: Rubriquefrais;
  private ids;

  // Définition des LOV

  constructor(
	private _listeService: ListeService,         // Pour gestion des listes
    private _rubriquefraisService: RubriquefraisService,
    private _route: ActivatedRoute,
    private _router: Router,
    private _formBuilder: FormBuilder,
    private _notificationService: NotificationService,
    public dialog: MatDialog) {
		super(_listeService,_notificationService);
 	}

  ngOnInit() {
    this.getIdFromRouteParams();
    this.initForm();
  }

  getIdFromRouteParams = () => {
    this._route.params.subscribe(p => {
        this.ids = _.values(p);
    });
  }

  initForm = () => {
    this.form = this._formBuilder.group(this.getNewForm());
    if (!_.isEmpty(this.ids)) {
      this.load();
    }

	/* Chargement des LOVs */
  }

  getNewForm = (rubriquefrais?: Rubriquefrais) => {
    return {
      code: [
          (rubriquefrais ? rubriquefrais.code : ''),
          Validators.required
      ],
      nom: [
        (rubriquefrais ? rubriquefrais.nom : ''),
        Validators.required
      ],
      ordre: [
        (rubriquefrais ? rubriquefrais.ordre : '')
      ],
      ccstructure: [
        (rubriquefrais ? rubriquefrais.ccstructure : '')
      ],
      cccommercial: [
        (rubriquefrais ? rubriquefrais.cccommercial : '')
      ],
      ccoperationnel: [
        (rubriquefrais ? rubriquefrais.ccoperationnel : '')
      ],
      ccrefact: [
        (rubriquefrais ? rubriquefrais.ccrefact : '')
      ],
      image: [
        (rubriquefrais ? rubriquefrais.image : '')
      ]
    };
  }


  load = () => {
	// Concaténer les ids mais séparé par / et non ,
	var param : String = '';
	
	for (var i=0;i<this.ids.length;i++) {
		param = param + this.ids[i];
		if (i != (this.ids.length-1)) {
			param = param + '/';
		}
	}
    this._rubriquefraisService.get(param).subscribe(
      (rubriquefrais: Rubriquefrais) => {
        this.rubriquefrais = rubriquefrais;
        this.form = this._formBuilder.group(this.getNewForm(this.rubriquefrais));
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
 	  },
      () => {
		// Valorisation des filtres
      });
  }

  save = () => {
	// If we didn't get a rubriquefrais, we are adding a rubriquefrais
    if (!this.rubriquefrais) {
      this.add();
    } else { // If we didn't get a rubriquefrais, we are adding a rubriquefrais
      this.update();
    }
  }

  add = () => {
    this._rubriquefraisService.create(this.form.value).subscribe(
      result => {
        this._notificationService.success('Success', 'Rubriquefrais added successfuly');
        this._router.navigate(['./rubriquefrais-form', this.form.value.code]);
      },
      error => {1
        if (error.status === this.CONFLICT_ERROR) {
          this._notificationService.error(error.statusText, 'Id already used in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  update = () => {
	Object.assign(this.rubriquefrais,this.form.value);
    this._rubriquefraisService.update(this.rubriquefrais).subscribe(
      result => this._notificationService.success('Success', 'Rubriquefrais edited successfuly'),
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  deleteConfirmation = () => {
    if (this.rubriquefrais.code) {
      const dialogRef = this.dialog.open(DeleteDialogComponent, {
        data: `rubriquefrais: ${this.rubriquefrais.code}`
      });
      dialogRef.afterClosed().subscribe(result => {
        if (result) {
          this.deleteBadge(this.rubriquefrais);
        }
      });
    }
  }

  deleteBadge = (rubriquefrais: Rubriquefrais): void => {
    // Call delete service
    this._rubriquefraisService.delete(rubriquefrais).subscribe(
      result => {
        this._router.navigate(['./rubriquefrais-list']);

        this._notificationService.success(
          'Deleted',
         // `The rubriquefrais entry with the id(s)='${code}' was deleted successfuly`);
 		`The rubriquefrais entry with the id= was deleted successfuly`);
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }
}
