// File generated by Telosys Tools Generator ( version 3.1.2 ) - Date 2020-04-27 ( Time 15:44:21 )

// Modules imports
import { NotificationService } from './../../../services/notification.service';
import { FormBuilder, FormGroup, Validators } from '@angular/forms';
import { MatDialog } from '@angular/material';
import { ActivatedRoute, Router } from '@angular/router';
import { Component, OnInit } from '@angular/core';

// Components imports
import { DeleteDialogComponent } from './../../../delete-dialog/delete-dialog.component';

import { DatePipe } from '@angular/common';

// Models and services imports
import { Contrat } from './../contrat.model';
import { ContratService } from './../services/contrat.service';
import * as _ from 'underscore';

// Pour gestion des listes
import { ListeService } from './../../../entities/liste/services/liste.service';
import { Liste } from './../../../entities/liste/liste.model';

import { Lov } from './../../../common/lov';
import {SelectItem} from 'primeng/api';

@Component({
  selector: 'app-contrat-form',
  templateUrl: './contrat-form.component.html',
  styleUrls: ['./contrat-form.component.css'],
  providers: [DatePipe]
})
export class ContratFormComponent extends Lov implements OnInit {

  // HTTP status code
  readonly NOT_FOUND_ERROR = 404;
  readonly CONFLICT_ERROR = 409;
  readonly INTERNAL_SERVER_ERROR = 500;

  public title = 'Formulaire Contrat';
  public form: FormGroup;

  // Définition des filtres

  public contrat: Contrat;
  private ids;

  // Définition des LOV

  constructor(
	private _listeService: ListeService,         // Pour gestion des listes
    private _contratService: ContratService,
    private _route: ActivatedRoute,
    private _router: Router,
    private _formBuilder: FormBuilder,
    private _notificationService: NotificationService,
    public dialog: MatDialog) {
		super(_listeService,_notificationService);
 	}

  ngOnInit() {
    this.getIdFromRouteParams();
    this.initForm();
  }

  getIdFromRouteParams = () => {
    this._route.params.subscribe(p => {
        this.ids = _.values(p);
    });
  }

  initForm = () => {
    this.form = this._formBuilder.group(this.getNewForm());
    if (!_.isEmpty(this.ids)) {
      this.load();
    }

	/* Chargement des LOVs */
  }

  getNewForm = (contrat?: Contrat) => {
    return {
      contratid: [{
          value: (contrat ? contrat.contratid : ''),
          disabled: true
      }],      reference: [
        (contrat ? contrat.reference : '')
      ],
      idclient: [
        (contrat ? contrat.idclient : ''),
        Validators.required
      ],
      idclientsec: [
        (contrat ? contrat.idclientsec : ''),
        Validators.required
      ],
      contact1: [
        (contrat ? contrat.contact1 : '')
      ],
      tel1Portable: [
        (contrat ? contrat.tel1Portable : '')
      ],
      tel1Travail: [
        (contrat ? contrat.tel1Travail : '')
      ],
      tel1Domicile: [
        (contrat ? contrat.tel1Domicile : '')
      ],
      datedebut: [
        (contrat ? contrat.datedebut : null)
      ],
      datefin: [
        (contrat ? contrat.datefin : null)
      ],
      referenceclient: [
        (contrat ? contrat.referenceclient : '')
      ],
      type: [
        (contrat ? contrat.type : '')
      ],
      etatComm: [
        (contrat ? contrat.etatComm : '')
      ],
      signature: [
        (contrat ? contrat.signature : '')
      ],
      avancement: [
        (contrat ? contrat.avancement : '')
      ],
      nbjPrev: [
        (contrat ? contrat.nbjPrev : '')
      ],
      adressefact: [
        (contrat ? contrat.adressefact : '')
      ],
      contactfact: [
        (contrat ? contrat.contactfact : '')
      ],
      avenant: [
        (contrat ? contrat.avenant : ''),
        Validators.required
      ],
      categorie: [
        (contrat ? contrat.categorie : ''),
        Validators.required
      ],
      mail1: [
        (contrat ? contrat.mail1 : '')
      ],
      factcra: [
        (contrat ? contrat.factcra : '')
      ],
      datereception: [
        (contrat ? contrat.datereception : null)
      ],
      daterenvoi: [
        (contrat ? contrat.daterenvoi : null)
      ],
      dateretour: [
        (contrat ? contrat.dateretour : null)
      ]
    };
  }


  load = () => {
	// Concaténer les ids mais séparé par / et non ,
	var param : String = '';
	
	for (var i=0;i<this.ids.length;i++) {
		param = param + this.ids[i];
		if (i != (this.ids.length-1)) {
			param = param + '/';
		}
	}
    this._contratService.get(param).subscribe(
      (contrat: Contrat) => {
        this.contrat = contrat;
        this.form = this._formBuilder.group(this.getNewForm(this.contrat));
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
 	  },
      () => {
		// Valorisation des filtres
      });
  }

  save = () => {
	// If we didn't get a contrat, we are adding a contrat
    if (!this.contrat) {
      this.add();
    } else { // If we didn't get a contrat, we are adding a contrat
      this.update();
    }
  }

  add = () => {
    this._contratService.create(this.form.value).subscribe(
      result => {
        this._notificationService.success('Success', 'Contrat added successfuly');
        this._router.navigate(['./contrat-form', this.form.value.contratid]);
      },
      error => {1
        if (error.status === this.CONFLICT_ERROR) {
          this._notificationService.error(error.statusText, 'Id already used in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  update = () => {
	Object.assign(this.contrat,this.form.value);
    this._contratService.update(this.contrat).subscribe(
      result => this._notificationService.success('Success', 'Contrat edited successfuly'),
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  deleteConfirmation = () => {
    if (this.contrat.contratid) {
      const dialogRef = this.dialog.open(DeleteDialogComponent, {
        data: `contrat: ${this.contrat.contratid}`
      });
      dialogRef.afterClosed().subscribe(result => {
        if (result) {
          this.deleteBadge(this.contrat);
        }
      });
    }
  }

  deleteBadge = (contrat: Contrat): void => {
    // Call delete service
    this._contratService.delete(contrat).subscribe(
      result => {
        this._router.navigate(['./contrat-list']);

        this._notificationService.success(
          'Deleted',
         // `The contrat entry with the id(s)='${contratid}' was deleted successfuly`);
 		`The contrat entry with the id= was deleted successfuly`);
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }
}
