// File generated by Telosys Tools Generator ( version 3.1.2 ) - Date 2020-04-27 ( Time 15:44:25 )

// Modules imports
import { NotificationService } from './../../../services/notification.service';
import { FormBuilder, FormGroup, Validators } from '@angular/forms';
import { MatDialog } from '@angular/material';
import { ActivatedRoute, Router } from '@angular/router';
import { Component, OnInit } from '@angular/core';

// Components imports
import { DeleteDialogComponent } from './../../../delete-dialog/delete-dialog.component';

import { DatePipe } from '@angular/common';

// Models and services imports
import { Echeancecontratfournisseur } from './../echeancecontratfournisseur.model';
import { EcheancecontratfournisseurService } from './../services/echeancecontratfournisseur.service';
import * as _ from 'underscore';

// Pour gestion des listes
import { ListeService } from './../../../entities/liste/services/liste.service';
import { Liste } from './../../../entities/liste/liste.model';

import { Lov } from './../../../common/lov';
import {SelectItem} from 'primeng/api';

@Component({
  selector: 'app-echeancecontratfournisseur-form',
  templateUrl: './echeancecontratfournisseur-form.component.html',
  styleUrls: ['./echeancecontratfournisseur-form.component.css'],
  providers: [DatePipe]
})
export class EcheancecontratfournisseurFormComponent extends Lov implements OnInit {

  // HTTP status code
  readonly NOT_FOUND_ERROR = 404;
  readonly CONFLICT_ERROR = 409;
  readonly INTERNAL_SERVER_ERROR = 500;

  public title = 'Formulaire Echeancecontratfournisseur';
  public form: FormGroup;

  // Définition des filtres

  public echeancecontratfournisseur: Echeancecontratfournisseur;
  private ids;

  // Définition des LOV

  constructor(
	private _listeService: ListeService,         // Pour gestion des listes
    private _echeancecontratfournisseurService: EcheancecontratfournisseurService,
    private _route: ActivatedRoute,
    private _router: Router,
    private _formBuilder: FormBuilder,
    private _notificationService: NotificationService,
    public dialog: MatDialog) {
		super(_listeService,_notificationService);
 	}

  ngOnInit() {
    this.getIdFromRouteParams();
    this.initForm();
  }

  getIdFromRouteParams = () => {
    this._route.params.subscribe(p => {
        this.ids = _.values(p);
    });
  }

  initForm = () => {
    this.form = this._formBuilder.group(this.getNewForm());
    if (!_.isEmpty(this.ids)) {
      this.load();
    }

	/* Chargement des LOVs */
  }

  getNewForm = (echeancecontratfournisseur?: Echeancecontratfournisseur) => {
    return {
      echeancecontratfournisseurid: [{
          value: (echeancecontratfournisseur ? echeancecontratfournisseur.echeancecontratfournisseurid : ''),
          disabled: true
      }],      idcontratfournisseur: [
        (echeancecontratfournisseur ? echeancecontratfournisseur.idcontratfournisseur : ''),
        Validators.required
      ],
      dateecheance: [
        (echeancecontratfournisseur ? echeancecontratfournisseur.dateecheance : null)
      ],
      libelle: [
        (echeancecontratfournisseur ? echeancecontratfournisseur.libelle : '')
      ],
      montantht: [
        (echeancecontratfournisseur ? echeancecontratfournisseur.montantht : '')
      ],
      tauxtva: [
        (echeancecontratfournisseur ? echeancecontratfournisseur.tauxtva : '')
      ]
    };
  }


  load = () => {
	// Concaténer les ids mais séparé par / et non ,
	var param : String = '';
	
	for (var i=0;i<this.ids.length;i++) {
		param = param + this.ids[i];
		if (i != (this.ids.length-1)) {
			param = param + '/';
		}
	}
    this._echeancecontratfournisseurService.get(param).subscribe(
      (echeancecontratfournisseur: Echeancecontratfournisseur) => {
        this.echeancecontratfournisseur = echeancecontratfournisseur;
        this.form = this._formBuilder.group(this.getNewForm(this.echeancecontratfournisseur));
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
 	  },
      () => {
		// Valorisation des filtres
      });
  }

  save = () => {
	// If we didn't get a echeancecontratfournisseur, we are adding a echeancecontratfournisseur
    if (!this.echeancecontratfournisseur) {
      this.add();
    } else { // If we didn't get a echeancecontratfournisseur, we are adding a echeancecontratfournisseur
      this.update();
    }
  }

  add = () => {
    this._echeancecontratfournisseurService.create(this.form.value).subscribe(
      result => {
        this._notificationService.success('Success', 'Echeancecontratfournisseur added successfuly');
        this._router.navigate(['./echeancecontratfournisseur-form', this.form.value.echeancecontratfournisseurid]);
      },
      error => {1
        if (error.status === this.CONFLICT_ERROR) {
          this._notificationService.error(error.statusText, 'Id already used in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  update = () => {
	Object.assign(this.echeancecontratfournisseur,this.form.value);
    this._echeancecontratfournisseurService.update(this.echeancecontratfournisseur).subscribe(
      result => this._notificationService.success('Success', 'Echeancecontratfournisseur edited successfuly'),
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }

  deleteConfirmation = () => {
    if (this.echeancecontratfournisseur.echeancecontratfournisseurid) {
      const dialogRef = this.dialog.open(DeleteDialogComponent, {
        data: `echeancecontratfournisseur: ${this.echeancecontratfournisseur.echeancecontratfournisseurid}`
      });
      dialogRef.afterClosed().subscribe(result => {
        if (result) {
          this.deleteBadge(this.echeancecontratfournisseur);
        }
      });
    }
  }

  deleteBadge = (echeancecontratfournisseur: Echeancecontratfournisseur): void => {
    // Call delete service
    this._echeancecontratfournisseurService.delete(echeancecontratfournisseur).subscribe(
      result => {
        this._router.navigate(['./echeancecontratfournisseur-list']);

        this._notificationService.success(
          'Deleted',
         // `The echeancecontratfournisseur entry with the id(s)='${echeancecontratfournisseurid}' was deleted successfuly`);
 		`The echeancecontratfournisseur entry with the id= was deleted successfuly`);
      },
      error => {
        if (error.status === this.NOT_FOUND_ERROR) {
          this._notificationService.error(error.statusText, 'Entity not found in database');
        } else if (error.status === this.INTERNAL_SERVER_ERROR) {
          this._notificationService.error(error.statusText, error.json());
        } else {
          this._notificationService.error('Error', 'An error occured when trying to reach the server');
        }
      });
  }
}
