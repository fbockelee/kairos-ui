##
#set( $jdbcTool = $loader.newInstance("JdbcTool") )
$jdbcTool.setAllFromConfig($project.specificVariables)##
$jdbcTool.setOrderby("ORDER BY ext6")##
#set( $extAttributes=$jdbcTool.getExtFieldAttributes($entity))
##
#set ( $LCentityName = $fn.uncapitalize($entity.name) )
#set ($idsPrefix = "${LCentityName}.")
<!-- File generated by $generator.name ( version $generator.version ) - Date $today.date("yyyy-MM-dd") ( Time $today.time ) --> 

 	<p-table [value]="listOf${entity.name}s" selectionMode="single" [scrollable]="true" scrollHeight="450px" [rowHover]="true">
        <ng-template pTemplate="caption">
        	<button pButton type="button" label="Créer" class="ui-button-rounded" routerLink="../${LCentityName}-form"></button>
        </ng-template>
		<ng-template pTemplate="header">
			<tr>
#foreach ( $extattrib in $extAttributes)
#set ($attrib = $entity.attributes.get($extattrib.index))
		        <th [ngStyle]="{'display': columnDefinitions[$foreach.index].display}">${extattrib.ext1}</th>
#end
		        <th [ngStyle] = "{'width': '50px'}"></th>
		    </tr>
		</ng-template>
		<ng-template pTemplate="body" let-$LCentityName>
		    <tr class="ui-selectable-row" (click)="edit${entity.name}(${entity.keyAttributesNamesAsString(', ', $idsPrefix, '')})">
#foreach ( $extattrib in $extAttributes)
#set ($attrib = $entity.attributes.get($extattrib.index))
#if ( $attrib.isDateType() )
		        <td [ngStyle]="{'display': columnDefinitions[$foreach.index].display}">{{${idsPrefix}${attrib.name} | date:'dd/MM/yyyy'}}</td>
#elseif ( $attrib.isBooleanType())
				<td [ngStyle]="{'display': columnDefinitions[$foreach.index].display}"><p-checkbox [(ngModel)]="${idsPrefix}${attrib.name}" binary="true" readonly></p-checkbox></td>
#else
		        <td [ngStyle]="{'display': columnDefinitions[$foreach.index].display}">{{${idsPrefix}${attrib.name}}}</td>
#end	
#end
		        <td [ngStyle] = "{'width': '50px'}">
		            <button pButton type="button" class="ui-button-danger " (click)="deleteConfirmation(${entity.keyAttributesNamesAsString(', ', $idsPrefix, '')}); ${DOLLAR}event.stopPropagation();" icon="fa fa-trash" title="Supprimer"
                            pTooltip="Supprimer" tooltipPosition="top"></button>
                </td>
		    </tr>
		</ng-template>
	</p-table>    
